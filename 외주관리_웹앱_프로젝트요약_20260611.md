# 외주관리 웹앱 — 프로젝트 요약
> 기존 복잡한 외주관리 → 단순화 재설계 + 구글시트 연동
> 업데이트: 2026-06-11 | 다음 대화 시 이 문서를 공유하면 맥락 유지 가능

---

## 1. 프로젝트 개요

- **목적**: 외주 임가공 발주 건을 간단하게 추적·관리
- **변경 이력**: 기존 `outsource.html` (복잡한 구조, Google Sheets API v4 방식) → 완전 재설계
- **단순화 방향**: 발주번호(PO) 제거, 고객사 제거, 핵심 7개 칼럼으로 축소

---

## 2. 접속 정보

| 항목 | 내용 |
|------|------|
| **URL** | `https://caddis-gr.github.io/caddis-laser/outsource.html` |
| **GitHub** | `caddis-gr/caddis-laser` 리포 → `outsource.html` |
| **구글시트** | `외주관리_CADDIS레이저` |
| **시트 ID** | `1I-ah526f4nRxI_cUxJRpr0GTNf_x2uFrDFI5qknxdUU` |
| **Apps Script URL** | `https://script.google.com/macros/s/AKfycbymTedUYYrn5M-q5MHWPq-X2EGau4y2fEzy4Oh4tbuFxv1096GzHdQ0As3KW2YEST0B/exec` |

---

## 3. 구현된 기능

| 기능 | 내용 |
|------|------|
| 시트 연동 | 추가·수정·삭제 → 구글시트 즉시 저장, 새로고침 유지 |
| 건 CRUD | + 새 건 등록 / 행 클릭 → 수정·삭제 모달 |
| 상태 필터 | 견적중·발주완료·진행중·납품완료 칩 필터 |
| 건명·업체 검색 | 실시간 검색 |
| 납기 D-day | 발주완료·진행중 건이 D-3 이내면 황색 + D-day 표시 |
| 명세표 링크 | `파일명 \| 드라이브링크` → 클릭 시 새 탭 오픈 |
| 내보내기 | 현재 현황 JSON 스냅샷 다운로드 |

---

## 4. 데이터 구조

### 구글시트 칼럼 (9칸)
| A | B | C | D | E | F | G | H | I |
|---|---|---|---|---|---|---|---|---|
| id | 건명번호 | 건명 | 외주업체 | 상태 | 납기일 | 발주금액 | 명세표 | 비고 |

### 작업 객체 (JavaScript)
```javascript
{
  id:      'J1749123456789',      // 자동 생성 (J + timestamp)
  no:      'JOB-2026-008',        // 건명번호 — 수기 입력
  name:    'ON FENDER 파이프 레이저',
  vendor:  '○○레이저',
  stage:   'ord',                 // est / ord / prog / done
  due:     '2026-06-17',
  amount:  320000,
  invoice: [                      // 명세표
    { name: '명세표_20260611.pdf', url: 'https://drive.google.com/...' },
    { name: '영수증.pdf', url: '' } // 링크 없으면 텍스트만
  ],
  note:    '특이사항'
}
```

### 진행 단계
```
견적중(est) → 발주완료(ord) → 진행중(prog) → 납품완료(done)
```

### 명세표 입력 형식 (모달 안)
```
명세표_20260611.pdf | https://drive.google.com/file/d/XXXXX/view
영수증.pdf                ← 링크 없으면 이름만 표시
```
> `|` 입력: Shift + \ (백슬래시 키)

---

## 5. 기술 구조

```
[outsource.html]  ←fetch/POST→  [Apps Script]  ←→  [구글시트]
 GitHub Pages                    웹앱 URL             외주 탭
```

### Apps Script 핵심
- `doGet` → 시트 전체 읽기 → JSON 반환
- `doPost(add)` → 새 행 추가, id = `'J' + Date.now()`
- `doPost(update)` → id로 행 찾아 덮어쓰기
- `doPost(delete)` → id로 행 찾아 삭제
- CORS 우회: `Content-Type: text/plain` 으로 POST

---

## 6. Apps Script 재배포 방법

```
Apps Script 편집기
  → 배포 → 새 배포
  → 톱니바퀴(⚙️) → 웹 앱
  → 액세스: 모든 사용자
  → 배포 → 새 URL 복사
  → outsource.html 의 SHEETS_URL 교체
```
> ⚠️ 재배포 시 URL이 바뀌므로 HTML도 같이 업데이트 필요

---

## 7. 기존 버전과 차이점

| 항목 | 구 버전 | 신 버전 |
|------|---------|---------|
| 연결 방식 | Google Sheets API v4 (ID + API Key) | Apps Script (웹앱 URL) |
| 발주번호 | PO-YYYYMM-NNN 자동 채번 | 없음 (제거) |
| 건명번호 | 자동 채번 | **수기 입력** |
| 고객사 | 있음 | 없음 (제거) |
| 칼럼 수 | 14개 | 9개 |
| 뷰 | 건명대장 + 업체 + PO 패널 | 단일 테이블 |

---

## 8. 남은 작업

- [ ] `index.html` 메인 허브에 outsource 링크 확인·업데이트
- [ ] 월별 정산 집계 뷰 (필요 시)
- [ ] 모바일 뷰 최적화 (필요 시)

---

*관련 파일: `outsource.html` / `caddis-gr/caddis-laser` 리포*
*이 문서를 새 채팅창에 공유하면 맥락을 이어갈 수 있습니다.*

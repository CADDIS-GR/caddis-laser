# 작업 진행 현황판 웹앱 — 프로젝트 요약
> 업데이트: 2026-06-11 | 다음 대화 시 이 문서를 공유하면 맥락 유지 가능

---

## 1. 프로젝트 개요

- **목적**: 자체 생산품(레이저 절단) 작업 플로우를 혼자 관리하는 전용 현황판
- **출발**: Notion 자작 Work Flow Sheet → 자체 웹앱으로 이전
- **특징**: 주 사용자 1인, PC·폰 여러 기기에서 같은 데이터 공유, 필요 시 링크 공유

---

## 2. 현재 완성 상태 ✅

### 접속 URL
```
https://caddis-gr.github.io/caddis-laser/workflow-board.html
```

### 파일 위치
- **GitHub**: `caddis-gr/caddis-laser` 리포 → `workflow-board.html`
- **구글시트**: `작업진행현황판_CADDIS레이저`
  - 시트 ID: `1EjV-IeW6bPGJzp1R_NyHERU5fm6VIYcpRRCp7f0iCEQ`
- **Apps Script URL**: `https://script.google.com/macros/s/AKfycbz-pyl_hSe9tnNO9ZBMIjhOlBmF6t6PK4It-uoAE-k1uRZwuFC5W_cvCBXiCZ6L_K5m/exec`

### 구현된 기능
| 기능 | 내용 |
|------|------|
| 시트 연동 | 추가·수정·삭제 → 구글시트 즉시 저장, 새로고침 유지 |
| 3가지 뷰 | 표 / 보드(칸반) / 타임라인 전환 |
| 작업 CRUD | + 추가 / 행·카드 클릭 → 수정·삭제 모달 |
| 납기 D-day | 미완료 건 D-2 이내 황색 표시 |
| 상태 필터·검색 | 대기·진행·완료·납품 칩 필터 + 건명·내용 검색 |
| 작업지시서 링크 | `파일명 \| 드라이브링크` → 클릭 시 새 탭 오픈 |
| 다크·라이트 테마 | ◐ 버튼 전환 |
| 공유 내보내기 | JSON 스냅샷 다운로드 (↗ 공유 버튼) |

---

## 3. 데이터 구조

### 구글시트 칼럼 (10칸)
| A | B | C | D | E | F | G | H | I | J |
|---|---|---|---|---|---|---|---|---|---|
| id | 건명 | 시작일 | 납기일 | 작업내용 | 사용자재 | 작업지시서 | 자재비 | 가공비 | 단계 |

### 작업 객체 구조 (JavaScript)
```javascript
{
  id:       'J1749123456789',   // 자동 생성 (J + timestamp)
  job:      'DN8-OTR 브라켓',   // 건명
  start:    '2026-06-10',
  due:      '2026-06-11',
  work:     '레이저 절단 + 벤딩',  // 작업내용
  material: 'SS400-3t',           // 사용자재
  docs: [                         // 작업지시서
    { name: '900.pdf', url: 'https://drive.google.com/...' },
    { name: '901.pdf', url: '' }  // 링크 없으면 텍스트만
  ],
  mat:   53000,   // 자재비
  fee:   40000,   // 가공비
  stage: 'prog'   // wait / prog / done / ship
}
```

### 진행 단계
```
대기(wait) → 진행(prog) → 완료(done) → 납품(ship)
```

### 작업지시서 입력 형식 (모달 안)
```
900.pdf | https://drive.google.com/file/d/XXXXX/view
901.pdf                          ← 링크 없으면 이름만 표시
```

---

## 4. 기술 구조

```
[workflow-board.html]  ←fetch/POST→  [Apps Script]  ←→  [구글시트]
 GitHub Pages                         웹앱 URL              DB
 (화면·로직)                           (읽기/쓰기 다리)       (저장소)
```

### Apps Script 핵심 동작
- `doGet` → 시트 전체 읽기 → JSON 반환
- `doPost(add)` → 새 행 추가, id = `'J' + Date.now()`
- `doPost(update)` → id로 행 찾아 덮어쓰기
- `doPost(delete)` → id로 행 찾아 삭제
- CORS 우회: `Content-Type: text/plain` 으로 POST

### 연결 상태 배너
- `●` 시트에서 불러오는 중… (로딩)
- `●` 시트 연결됨 — 변경이 저장됩니다 (정상)
- `●` 샘플 데이터 — 시트 미연결 (SHEETS_URL 비어있을 때)
- `●` 시트 연결 실패 (오류)

---

## 5. Apps Script 재배포 방법

코드 수정 후 반드시 재배포해야 반영됩니다.
```
Apps Script 편집기
  → 배포 → 배포 관리
  → 연필(✏️) 아이콘
  → 버전: 새 버전 선택
  → 배포
```
> URL은 그대로 유지됩니다.

---

## 6. 자주 발생하는 문제

| 증상 | 원인 | 해결 |
|------|------|------|
| 클릭해도 모달 안 열림 | id 타입 불일치 (숫자 vs 문자열) | String() 비교로 수정됨 ✅ |
| 새로고침하면 초기화 | SHEETS_URL 비어있거나 연결 실패 | URL·배포 권한 확인 |
| 시트 연결 실패 배너 | 배포 액세스 권한 오류 | "모든 사용자"로 재배포 |
| 로딩 중 샘플 데이터 노출 | DATA 배열에 샘플 있었음 | 빈 배열로 수정됨 ✅ |
| 칼럼 순서 불일치 | Apps Script HEADERS와 시트 헤더 다름 | 둘 다 동일하게 유지 |

---

## 7. 남은 작업

### 단기
- [ ] 외주관리 시스템(`outsource.html`) 구글시트 연결 — 같은 방식 적용
- [ ] `index.html` 메인 허브에 workflow-board 링크 추가

### 중기
- [ ] 월별/기간별 필터 추가
- [ ] 정산(자재비+가공비) 합계 집계 뷰
- [ ] 모바일 뷰 최적화

---

*관련 파일: `workflow-board.html` / `caddis-gr/caddis-laser` 리포*
*이 문서를 새 채팅창에 공유하면 맥락을 이어갈 수 있습니다.*

# 작업 진행 현황판 웹앱 — 프로젝트 요약
> Notion 자작 현황판 → 자체 웹앱 이전 프로젝트
> 최종 업데이트: 2026-06-11 | 다음 대화 시 이 문서를 공유하면 맥락 유지 가능

---

## 1. 프로젝트 배경

- **출발점**: 낚시대장이 Notion으로 직접 만든 `24-03월 Work Flow Sheet` (작업 진행 현황판)
  - 업체명·납기·작업지시서(PDF)·상태·자재·작업내용·자재비·가공비·매출액을 한 행에 담은 DB
  - Default / 보드 / 캘린더 / 타임라인 / 리스트 등 다중 뷰 구성
  - 상태 플로우: Not started → In progress → Done (+ 납품)
- **문제**: Claude가 Notion에 직접 접근(읽기·쓰기) 불가 → 보완 작업이 어려움
- **결정**: Notion을 떠나 **자체 웹앱으로 이전** (GitHub Pages 생태계와 일관성)

---

## 2. 확정된 설계 방향

핵심 질의응답으로 좁혀진 요구사항:

| 항목 | 결정 |
|------|------|
| 주 사용자 | **본인 1인** (담당자 스스로 플로우 관리가 메인) |
| 공유 | 보조적 — 필요할 때만 스냅샷 내보내기 |
| 사용 기기 | **PC·폰 둘 다 고르게** |
| 데이터 동기화 | **여러 기기에서 같은 데이터** 필요 → 브라우저 저장(localStorage) 탈락 |
| 데이터 저장소 | **Google Sheets + Apps Script** (카드 빌링 자동화와 동일 구조) |
| PDF 첨부 | **"파일명 \| 링크" 방식** — 클릭하면 새 탭에서 열림, 링크는 선택 |

### 저장소 방식 비교 결과
- ❌ 브라우저 localStorage/IndexedDB → 기기마다 데이터 따로 놀아서 탈락
- ❌ GitHub JSON 파일 → 수시 상태변경 현황판엔 편집 부담 커서 부적합
- ⭕ **Google Sheets + Apps Script** → 익숙한 재료, PC·폰 동기화, 가장 빠르고 안정적
- (외부 DB Supabase/Firebase는 현재 규모엔 과함)

---

## 3. 현재 완성 상태 (1단계)

### 파일: `workflow-board.html`

**단일 HTML 파일**로 구현된 화면 프로토타입. 현재 화면 안 **샘플 데이터로만 동작**
(새로고침 시 초기화 — 2단계에서 구글시트 연동으로 교체 예정).

#### 디자인
- CADDIS-GR 생태계 결: Noto Sans KR / DM Serif Display / DM Mono
- CSS 변수 팔레트, **다크·라이트 테마 전환** (◐ 버튼)
- 차분하고 밀도 있는 실무형

#### 구현된 기능
| 기능 | 내용 |
|------|------|
| 집계 카드 | 대기·진행·완료·납품 4단계 건수 상단 표시 |
| 3가지 뷰 | **표 / 보드(칸반) / 타임라인** 전환 |
| 작업 CRUD | + 추가 / 행·카드 클릭 시 수정·삭제 (모달) |
| 입력 항목 | 업체명·시작일·납기일·작업내용·작업지시서·자재비·가공비·단계 |
| 필터·검색 | 상태별 칩 필터 + 업체·내용 검색 |
| 납기 임박 표시 | 미완료(대기·진행) 건이 D-2 이내면 황색 + D-day 표시 |
| 작업지시서 링크 | `파일명 \| 링크` 입력 → 클릭 시 새 탭으로 열림 (↗) / 링크 없으면 이름만 |
| 공유(내보내기) | 현재 현황을 JSON 스냅샷 파일로 다운로드 (길목만 터둠) |

#### 상태 단계 (4단계)
```
대기(wait) → 진행(prog) → 완료(done) → 납품(ship)
```
- Notion 영문(Not started/In progress/Done) → 한글 4단계로 재구성
- ⚠️ 실제 업무 단계와 맞는지 **검토 필요** (예: 출하·검수 단계 추가 여부)

---

## 4. 데이터 구조 (현재 샘플 기준)

```javascript
{
  id:    1,
  cust:  '조율산업',              // 업체명
  start: '2024-03-15',           // 시작일
  due:   '2024-03-20',           // 납기일
  work:  'SV FRT RR DR OTR...',  // 작업내용
  docs:  [                       // 작업지시서 (파일명+링크)
    {name:'jo-5030.pdf', url:'https://drive.google.com/...'},
    {name:'jo-5050.pdf', url:''}  // 링크 없으면 텍스트만
  ],
  mat:   0,                      // 자재비
  fee:   0,                      // 가공비
  stage: 'done'                  // 진행 단계 (wait/prog/done/ship)
}
```

---

## 5. 검토 요청 사항 (낚시대장 확인 필요)

- [ ] **단계 이름·개수**가 실제 업무와 맞는지 (납품 외 출하·검수 등 필요?)
- [ ] **표 칼럼** 누락 여부 — Notion엔 No·자재선택·사용자재·매출액도 있었음
      (현재는 자재비+가공비=정산으로 합침)
- [ ] **색감·글꼴** 등 전반 인상이 CADDIS 결과 맞는지

---

## 6. 다음 작업 (2단계)

### 핵심: Google Sheets + Apps Script 연동
- [ ] 구글시트 1장 = DB (작업 건 1행씩 누적)
- [ ] Apps Script로 웹앱 ↔ 시트 읽기/쓰기 (카드 빌링 자동화 패턴 재사용)
- [ ] 웹앱의 `DATA` 샘플 배열 → 시트 fetch로 교체
- [ ] PC에서 추가한 작업이 폰에서도 동일하게 보이도록 동기화 확인

### 이후
- [ ] GitHub Pages 배포 → 링크 하나로 어디서든 접근
- [ ] (선택) PWA화 → 폰 홈화면 앱처럼 사용
- [ ] (선택) 공유 스냅샷 내보내기 기능 다듬기
- [ ] (선택) 거래명세표 링크 칼럼 추가

---

## 7. 작업 순서 원칙 (2단계로 끊은 이유)

```
1단계 (현재 완료): 화면 먼저 — 샘플 데이터로 모양·동작 확정
        ↓ OK 확인 후
2단계: 구글시트 연결 — 데이터 영속·동기화
```
한 번에 다 엮으면 어디서 틀어졌는지 찾기 어려우므로 단계로 분리.
(자재관리/외주관리 시스템의 "shape 먼저, persistence 나중" 패턴과 동일)

---

*관련 파일: `workflow-board.html`*
*이 문서를 새 채팅창에 공유하면 맥락을 이어갈 수 있습니다.*

# CADDIS 디자인 리뉴얼 — 프로젝트 요약
> Hybrid Dark + MiniMax 디자인 시스템 적용
> 업데이트: 2026-06-24 | 다음 대화 시 이 문서를 공유하면 맥락 유지 가능

---

## 1. 배경 및 결론

- **출발**: workflow-board.html을 MiniMax 디자인 MD(`DESIGN-minimax.md`) 기반으로 리뉴얼 시도
- **MiniMax 라이트 단독** → 실무 현황판엔 부적합 (화이트 캔버스, 장시간 눈 피로)
- **최종 결론**: **다크 베이스 + MiniMax 토큰 하이브리드** + ◐ 라이트/다크 토글

### 하이브리드 방향 요약
| 항목 | 선택 | 이유 |
|------|------|------|
| 배경 | 다크(`#0d1117`) | 장시간 가독성, 실무 도구 문법 |
| 폰트 | DM Sans (MiniMax) | Noto Sans KR 대체, 가독성 향상 |
| 버튼·칩·배지 | pill (`border-radius: 9999px`) | MiniMax 시그니처 |
| 상태 컬러 | MiniMax 팔레트 | coral·blue·green 선명함 |
| 모달 | 24px radius | MiniMax 히어로 카드 느낌 |
| 테마 전환 | localStorage 저장 | 마지막 선택 유지 |

---

## 2. 적용 파일

| 파일 | localStorage 키 | 상태 |
|------|----------------|------|
| `workflow-board.html` | `wf-theme` | ✅ 완료 |
| `outsource.html` | `os-theme` | ✅ 완료 |

---

## 3. 핵심 CSS 토큰

### 다크 (기본)
```css
--bg:#0d1117;  --panel:#161b22;  --panel-2:#1c2330;
--ink:#e6edf3; --ink-soft:#8b949e; --ink-faint:#484f58;
--accent:#3b82f6;
--st-prog:#3b82f6;  --st-done:#1ba673;
--st-ship:#ff5530;  --st-wait:#8b949e;
--nav-bg:rgba(13,17,23,.85);
```

### 라이트 (`[data-theme="light"]`)
```css
--bg:#f7f8fa;  --panel:#ffffff;  --panel-2:#f2f3f5;
--ink:#0a0a0a; --ink-soft:#5f5f5f;
--accent:#1456f0;
--nav-bg:rgba(255,255,255,.9);
```

### 공통 radius (MiniMax)
```
--r-sm:6px  --r-md:8px  --r-lg:12px
--r-xl:16px  --r-hero:24px  --r-full:9999px
```

---

## 4. 테마 토글 JS 표준 패턴

```javascript
$('#themeBtn').onclick = () => {
  const b = document.documentElement;
  const isLight = b.dataset.theme === 'light';
  b.dataset.theme = isLight ? 'dark' : 'light';
  $('#themeBtn').textContent = isLight ? '◐' : '◑';
  localStorage.setItem('PAGE-theme', b.dataset.theme);
};
(() => {
  const saved = localStorage.getItem('PAGE-theme');
  if (saved) {
    document.documentElement.dataset.theme = saved;
    $('#themeBtn').textContent = saved === 'light' ? '◑' : '◐';
  }
})();
```

> 페이지마다 고유 키 사용: `wf-theme`(현황판) / `os-theme`(외주관리)

---

## 5. 새 페이지 적용 절차

1. 폰트 링크 → DM Sans + DM Mono로 교체
2. `<style>` 전체 → 하이브리드 CSS로 교체
3. `<body data-theme="dark">` 속성 제거 (기본 다크)
4. 네비에 `#themeBtn` 버튼 추가
5. JS 끝에 테마 토글 코드 삽입
6. 문법 검증 후 GitHub 업로드

> ⚠️ JS 로직(시트 통신·CRUD·렌더링)은 절대 수정 금지

---

## 6. 스킬 저장

- **파일명**: `caddis-hybrid-design.skill`
- **트리거**: "디자인 리뉴얼", "스타일 적용", "UI 만들어줘", "화면 예쁘게", "디자인 통일"
- **내용**: CSS 변수 전체, 컴포넌트 스타일, 테마 토글 패턴, 적용 절차

---

*관련 파일: `workflow-board.html` / `outsource.html` / `caddis-hybrid-design.skill`*
*이 문서를 새 채팅창에 공유하면 맥락을 이어갈 수 있습니다.*
